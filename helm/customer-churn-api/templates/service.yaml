apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-churn-api.fullname" . }}
  labels:
    {{- include "customer-churn-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "customer-churn-api.selectorLabels" . | nindent 4 }}
