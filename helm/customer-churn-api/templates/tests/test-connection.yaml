apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "customer-churn-api.fullname" . }}-test-connection"
  labels:
    {{- include "customer-churn-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "customer-churn-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
