apiVersion: v1
kind: Service
metadata:
  name: customer-churn-api
  labels:
    app: customer-churn-api
spec:
  type: NodePort
  selector:
    app: customer-churn-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30081